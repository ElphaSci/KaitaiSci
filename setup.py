from distutils.core import setup
from setuptools import find_packages

setup(name='kaitaisci', version='1.0', packages=find_packages(), url='', license='', author='Elpha', author_email='', description='')
